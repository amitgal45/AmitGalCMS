import { SortForumsByCategoryPipe } from './sort-forums-by-category.pipe';

describe('SortForumsByCategoryPipe', () => {
  it('create an instance', () => {
    const pipe = new SortForumsByCategoryPipe();
    expect(pipe).toBeTruthy();
  });
});
